#/bin/bash

for FILE in $(git diff --name-only origin/master)
do
  extension=$("${FILE#*.}")
  if [ "$extension" == ".json" ]; then
    cat $FILE | python -m json.tool
    RC=$?

    if [ $RC -ne 0 ]; then
      echo "fix your json! -> ${FILE}"
      exit 1
    fi
  fi
done
